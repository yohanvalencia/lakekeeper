{
    "LABEL": {
        "name": "invalid-title",
        "color": "EEEEEE"
    },
    "CHECKS": {
        "regexp": "^(feat|fix|docs|chore)!?(\\(.*\\))?!?:.*"
    },
    "MESSAGES": {
        "success": "PR title is valid",
        "failure": "PR title is invalid",
        "notice": "PR Title needs to pass regex '^(feat|fix|docs|chore)!?(\\(.*\\))?!?:.*"
    }
}